name: PR meta

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  pr-meta:
    uses: ostara-labs/devtools/.github/workflows/pr-meta.yml@667a3e5c2bd1b770745e0f455076dafcba07b44a # v1.13.4
